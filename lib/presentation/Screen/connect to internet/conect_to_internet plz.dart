import 'package:flutter/material.dart';
class ConnectToInternet extends StatelessWidget {
  const ConnectToInternet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "connect to internet"
        ),
      ),

    );
  }
}
